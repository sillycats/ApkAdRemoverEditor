<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    app:cardBackgroundColor="?attr/colorSurface"
    app:cardCornerRadius="16dp"
    app:cardElevation="1dp"
    app:strokeColor="@color/blush_light"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp">

        <!-- 第一行：图标 + 标题（标题独占剩余宽度，自动换行完整显示，不用省略号）+ 帮助 + 开关 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <!-- 分类图标 -->
            <ImageView
                android:id="@+id/ivCategoryIcon"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@drawable/bg_icon_round"
                android:contentDescription="@string/s_3629f42b"
                android:padding="8dp"
                app:tint="@color/primary" />

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="10dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvCategoryName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="none"
                    android:breakStrategy="high_quality"
                    android:textStyle="bold"
                    android:textSize="14sp"
                    android:textColor="?attr/colorOnSurface"
                    android:text="@string/s_04d7d88e" />
            </LinearLayout>

            <ImageButton
                android:id="@+id/btnCategoryHelp"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="center_vertical"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/s_dc91b561"
                android:src="@drawable/ic_help"
                android:tint="@color/accent" />

            <!-- 分类启停开关：关闭后处理 APK 时跳过该分类的去广告 -->
            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/swCategoryEnabled"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginStart="2dp"
                android:checked="true"
                android:contentDescription="@string/s_5ad1196f"
                app:thumbTint="@color/primary"
                app:trackTint="@color/primary_light" />
        </LinearLayout>

        <!-- 第二行：统计信息 + 管理按钮（靠右） -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvCategoryCount"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="12sp"
                android:textColor="@color/accent"
                android:text="@string/s_67113ed9" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnManage"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:minWidth="0dp"
                android:maxLines="1"
                android:text="@string/s_08b55fea"
                android:textSize="13sp"
                android:textColor="@color/primary_dark"
                app:autoSizeMinTextSize="9sp"
                app:autoSizeMaxTextSize="13sp"
                app:autoSizeStepGranularity="0.5sp"
                app:autoSizeTextType="uniform" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>